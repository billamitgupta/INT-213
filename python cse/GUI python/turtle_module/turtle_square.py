import turtle
turtle.showturtle()
   
for y in range(60,35,-5):
    if(y==55):
        turtle.begin_fill()
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.fillcolor("black")
        turtle.end_fill()
    elif(y==50):
        turtle.begin_fill()
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.fillcolor("pink")
        turtle.end_fill()
    elif(y==45):
        turtle.begin_fill()
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.fillcolor("red")
        turtle.end_fill()
    elif(y==40):
        turtle.begin_fill()
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.left(90)
        turtle.forward(y)
        turtle.fillcolor("white")
        turtle.end_fill()